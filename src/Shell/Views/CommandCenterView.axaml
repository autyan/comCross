<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:ComCross.Shell.ViewModels"
             mc:Ignorable="d" d:DesignWidth="320" d:DesignHeight="600"
             x:Class="ComCross.Shell.Views.CommandCenterView"
             x:DataType="vm:CommandCenterViewModel">
    <StackPanel Spacing="12">
        <TextBlock Text="{Binding LocalizedStrings.ToolCommands}" FontSize="12" FontWeight="Bold" Foreground="#87909B" />

        <Border Background="#0F1417" BorderBrush="#25303A" BorderThickness="1" CornerRadius="6">
            <Grid>
                <TextBlock Text="{Binding LocalizedStrings.ToolCommandsEmpty}" Foreground="#87909B"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           IsVisible="{Binding Commands.Count, Converter={StaticResource IsZeroConverter}}" />
                <ListBox ItemsSource="{Binding Commands}"
                         SelectedItem="{Binding SelectedCommand}"
                         Background="#0F1417"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="8,6">
                                <TextBlock Text="{Binding Name}" FontSize="12" Foreground="#E6EDF3" />
                                <TextBlock Text="{Binding Group}" FontSize="11" Foreground="#87909B" />
                                <TextBlock Text="{Binding Hotkey}" FontSize="10" Foreground="#6C7580" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="{Binding LocalizedStrings.ToolCommandsSend}" Click="OnSendClick" />
            <Button Content="{Binding LocalizedStrings.ToolCommandsAdd}" Click="OnNewClick" />
            <Button Content="{Binding LocalizedStrings.ToolCommandsDelete}" Click="OnDeleteClick" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="{Binding LocalizedStrings.ToolCommandsImport}" Click="OnImportClick" Classes="secondary" />
            <Button Content="{Binding LocalizedStrings.ToolCommandsExport}" Click="OnExportClick" Classes="secondary" />
        </StackPanel>

        <Separator />

        <StackPanel Spacing="8">
            <TextBlock Text="{Binding LocalizedStrings.ToolCommandsName}" FontSize="12" Foreground="#B7C1CC" />
            <TextBox Text="{Binding EditorName}" />

            <TextBlock Text="{Binding LocalizedStrings.ToolCommandsPayload}" FontSize="12" Foreground="#B7C1CC" />
            <TextBox Text="{Binding EditorPayload}" AcceptsReturn="True" Height="80" />

            <StackPanel Orientation="Horizontal" Spacing="12">
                <StackPanel>
                    <TextBlock Text="{Binding LocalizedStrings.ToolCommandsType}" FontSize="12" Foreground="#B7C1CC" />
                    <ComboBox ItemsSource="{Binding PayloadTypeOptions}" SelectedItem="{Binding SelectedPayloadType}" Width="120">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Label}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="{Binding LocalizedStrings.ToolCommandsEncoding}" FontSize="12" Foreground="#B7C1CC" />
                    <TextBox Text="{Binding EditorEncoding}" Width="120" />
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <StackPanel>
                    <TextBlock Text="{Binding LocalizedStrings.ToolCommandsScope}" FontSize="12" Foreground="#B7C1CC" />
                    <ComboBox ItemsSource="{Binding ScopeOptions}" SelectedItem="{Binding SelectedScope}" Width="120">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Label}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="{Binding LocalizedStrings.ToolCommandsGroup}" FontSize="12" Foreground="#B7C1CC" />
                    <TextBox Text="{Binding EditorGroup}" Width="120" />
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <CheckBox Content="{Binding LocalizedStrings.ToolCommandsAppendCr}" IsChecked="{Binding EditorAppendCr}" />
                <CheckBox Content="{Binding LocalizedStrings.ToolCommandsAppendLf}" IsChecked="{Binding EditorAppendLf}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <StackPanel>
                    <TextBlock Text="{Binding LocalizedStrings.ToolCommandsHotkey}" FontSize="12" Foreground="#B7C1CC" />
                    <TextBox Text="{Binding EditorHotkey}" Width="160" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="{Binding LocalizedStrings.ToolCommandsSortOrder}" FontSize="12" Foreground="#B7C1CC" />
                    <TextBox Text="{Binding EditorSortOrder}" Width="80" />
                </StackPanel>
            </StackPanel>

            <Button Content="{Binding LocalizedStrings.ToolCommandsSave}" Classes="accent" Click="OnSaveClick" />
        </StackPanel>
    </StackPanel>
</UserControl>
