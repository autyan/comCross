# v0.5.0 Prerelease Review

## Review State

Status: passed.

Review branch:

```text
feature/v0.5.0_prerelease
```

Release code has already entered:

```text
main
develop
```

## Checklist

### 1. Current Release Development State

v0.5.0 has completed its intended release-engineering scope:

- Windows MSI package generation.
- Linux DEB/RPM package generation.
- Linux AppImage fallback package generation.
- Release checksum generation.
- Optional GPG signing for checksums.
- Windows MSI signing through GitHub Actions secrets.
- Official plugin package signing in release packaging.
- GitHub Actions release workflow validation.

The signed validation workflow has passed on GitHub Actions.

### 2. Mandatory Technical Debt

No remaining release-blocking technical debt is known for v0.5.0.

The async/await boundary cleanup was reviewed before release. Fake async awaits
were removed from the release branch scope, while real IO, process, IPC,
database, stream, timer, and dispatcher boundaries remain asynchronous.

### 3. Documentation Accuracy

Release documentation reflects the code and package behavior:

- `docs/release/notes/v0.5.0.md`
- `docs/release/changelog/v0.5.0.md`
- `docs/release/release-packaging-flow.md`
- `docs/specs/11-Packaging-Guide.md`
- `README.md`

Historical release notes and changelogs have also been normalized under
`docs/release/notes/` and `docs/release/changelog/`.

### 4. GitHub Actions Risk

Known GitHub Actions release risks have been handled:

- Signed Linux package generation was validated.
- Windows MSI generation and signing were validated.
- `release_notes_path` is required for final non-draft releases.
- Package checksum generation and optional signing are part of the final
  release workflow.
- Build output is warning-free under the local release gate.

Remaining accepted limitation:

- The Windows code-signing certificate is currently self-signed and is
  documented as such.

### 5. Next Development Stage Scope

v0.6.0 scope is documented in:

```text
docs/roadmap/v0.6.0-scope.md
```

The next stage focuses on custom protocol profiles, CPDL evolution, protocol
analyzer results, message store/search improvements, and the first official data
analysis consumer.

### 6. Release Documentation

v0.5.0 release documentation is ready:

```text
docs/release/notes/v0.5.0.md
docs/release/changelog/v0.5.0.md
```

Final release workflow should be triggered with:

```bash
gh workflow run release.yml \
  --ref release/v0.5.0 \
  -f version=0.5.0 \
  -f prerelease=false \
  -f draft=false \
  -f require_signing=true \
  -f release_notes_path=docs/release/notes/v0.5.0.md
```
